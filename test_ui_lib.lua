--by devix7 & gpt chat
    button.Size = UDim2.new(
        button.Size = UDim2
    
        button.Position = UDim
    local UI = {}
    
    function
        local button = Instance.
        button.Size = UDim2.
        button.Position = UDim2.
        button.
       
    
        button.Mouse
     UI.CreateButton(text, x, y, callback)
        local button = Instance.new("TextButton")
        button.Size = UDim2.new(0, 100, 0, 30)
        button.Position = UDim2.new(x, y)
        button.Text = text
        button.Parent = game:GetService("CoreGui")
        button.MouseButton1Click:Connect(callback)
    end
    
    function UI.CreateLabel(text, x, y)
        local label = Instance.
        local label = Instance.
    
        local label = Instance
    
        local label = Inst
    
        local label =
    
        local label
    
        local
    
       
    new("TextLabel")
        label.Size = UDim2.
        label.Size = UDim2.
    
        label.Size = UDim2
    
        label.Size = UDim
    
        label.Size = U
    
        label.Size =
    
        label.Size
    
        label.
    
        label
    
       
    new(0, 100, 0, 30)
        label.Position = UDim2.
        label.Position = UDim2.
    
        label.Position = UDim2
    
        label.Position = UDim
    
        label.Position = U
    
        label.Position =
    
        label.Position
    
        label.
    
        label
    
       
    new(x, y)
        label.
        label.
    
        label
    
       
    Text = text
        label.Parent = game:GetService(
        label.Parent = game:GetService
    
        label.Parent = game:
    
        label.Parent = game
    
        label.Parent =
    
        label.Parent
    
        label.
    
        label
    
       
    "CoreGui")
    end
    
    return UI